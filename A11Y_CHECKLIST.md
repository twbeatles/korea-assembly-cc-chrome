# 접근성 수동 체크리스트

키보드와 스크린 리더로 확장 UI를 확인할 때 사용한다. 자동화 스모크를 대체하지 않는다.

## History / Options / Popup

- [ ] Tab 만으로 주요 버튼(저장, 삭제, 내보내기, 설정 저장)에 도달한다.
- [ ] History 삭제·전체 삭제 확인은 `alertdialog` 이고 Esc 로 취소된다.
- [ ] 확인 대화상자를 닫으면 포커스가 원래 버튼으로 돌아간다.
- [ ] 오류 메시지·진행 상태가 `aria-live` 로 읽힌다.
- [ ] 숫자 입력 오류는 필드 `aria-invalid` 과 함께 표시된다.

## 페이지 안 패널

- [ ] 접기/펼치기, 자막 모으기, 멈추기, 화면 비우기, 복사가 키보드로 동작한다.
- [ ] 화면 비우기 확인 대화상자가 페이지 위에 나타나고 Esc 로 취소된다.
- [ ] `실시간 내용` / 공지는 polite live region 이다.
- [ ] `수집된 자막` 목록은 `role="log"` 이다.
- [ ] 고대비·강제 색 모드에서 글자와 버튼 경계가 보인다.

## 참고

- production 패널 Shadow 는 closed 이다. 페이지 AT가 내부를 못 읽는 조합이 있으면 팝업·History 로 동일 작업을 할 수 있는지 확인한다.
- e2e 마커(`data-assembly-e2e`)는 자동 테스트 전용이다. 일반 사용 경로에 켜 두지 않는다.
