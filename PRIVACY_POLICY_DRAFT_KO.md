# 개인정보처리방침 초안

이 문서는 Chrome Web Store 등록용 공개 링크로 게시할 수 있도록 작성한 초안입니다.  
아래 본문에서 `[운영자명]`, `[문의 이메일]`, `[시행일]`만 실제 정보로 바꿔서 Notion에 게시하면 됩니다.

---

## 개인정보처리방침

`국회 AI 자막 추출기`(이하 "확장프로그램")는 사용자의 개인정보와 브라우저 데이터를 투명하고 안전하게 처리하기 위해 다음과 같이 개인정보처리방침을 안내합니다.

### 1. 운영자 정보

- 운영자: `[운영자명 또는 개발자명]`
- 문의 이메일: `[문의 이메일]`

### 2. 처리하는 정보

확장프로그램은 다음 정보를 처리할 수 있습니다.

1. 사용자가 `https://assembly.webcast.go.kr/*` 또는 `https://webcast.assembly.go.kr/*` 페이지에서 보는 AI 자막 텍스트
2. 사용자가 확장프로그램에서 설정한 옵션 정보
   예: 자동 저장, 자동 스크롤, 파일명 패턴, 최근 N줄 복사 설정 등
3. 사용자가 직접 저장한 자막 세션 기록
4. 사용자가 기록에 직접 입력한 메모, 즐겨찾기 상태 같은 세션 메타데이터
5. 사용자가 직접 내보내기 또는 가져오기를 요청한 JSON/TXT/SRT/VTT 파일 데이터
6. 수집 상태, 저장 복구 상태, 최근 저장 시각, 프레임 간 메시지 무결성 검증에 필요한 최소 내부 상태 등 확장 내부 진단/동기화 정보

### 3. 정보를 처리하는 목적

확장프로그램은 다음 목적에 한해 정보를 처리합니다.

1. 국회 의사중계 페이지의 AI 자막을 화면에 표시하기 위해
2. 사용자가 자막 세션을 저장하고 다시 열람할 수 있도록 하기 위해
3. 사용자가 세션 메모와 즐겨찾기 상태를 기록과 함께 보관할 수 있도록 하기 위해
4. 사용자가 TXT, SRT, VTT, JSON 형식으로 자막을 내보내거나 JSON 백업을 다시 가져올 수 있도록 하기 위해
5. 사용자가 선택한 확장 설정을 유지하기 위해
6. 수집 상태와 저장 복구 상태를 사용자에게 진단 정보로 보여 주기 위해

### 4. 정보의 처리 방식

- 자막 텍스트와 설정 정보는 기본적으로 사용자의 브라우저 내부 저장소에 저장됩니다.
- 세션 메모와 즐겨찾기 상태 같은 사용자 입력 메타데이터도 브라우저 내부 저장소에 함께 저장됩니다.
- 저장소는 브라우저의 `IndexedDB` 및 `chrome.storage.local`을 사용합니다.
- 페이지 종료 직전 저장 복구를 위한 임시 대기 기록, history 화면 동기화를 위한 내부 revision 값, queue write / replay / cleanup 단계별 저장 복구 진단, 프레임 간 메시지 무결성 보호를 위한 탭 단위 nonce 같은 내부 상태도 같은 브라우저 로컬 저장소에서만 처리됩니다.
- 파일 내보내기는 사용자가 직접 저장 버튼을 눌렀을 때만 동작합니다.
- 수동 저장 / 파일 내보내기에는 사용자가 현재 패널에서 보고 있는 `수집된 자막` 목록이 우선 반영되며, 목록이 비어 있으면 현재 `실시간 내용`이 함께 포함될 수 있습니다.
- JSON 가져오기는 사용자가 history 화면에서 직접 파일을 선택했을 때만 동작하며, 허용된 형식과 필드만 복원합니다.
- 확장프로그램은 자막 데이터나 설정 정보를 개발자 서버 또는 제3자 서버로 전송하지 않습니다.

### 5. 제3자 제공 및 외부 전송

확장프로그램은 사용자가 보는 자막 텍스트, 저장된 세션 기록, 설정 정보를 판매하거나 제3자에게 제공하지 않습니다.

또한 다음 항목을 수행하지 않습니다.

- 회원가입 수집
- 광고 식별자 수집
- 분석/추적용 외부 전송
- 별도 백엔드 서버 업로드

### 6. 보관 및 삭제

- 확장프로그램이 저장한 설정 및 세션 기록은 사용자의 브라우저 내부에만 보관됩니다.
- 사용자는 확장프로그램의 기록 삭제 기능 또는 브라우저 확장 삭제를 통해 관련 데이터를 제거할 수 있습니다.
- 사용자가 직접 내보낸 파일은 사용자 기기의 다운로드 위치에 저장되며, 해당 파일 관리는 사용자가 직접 수행합니다.

### 7. 권한 사용

확장프로그램은 아래 목적을 위해 최소한의 권한만 사용합니다.

- `storage`: 설정, 저장된 자막 세션 기록, 저장 복구용 임시 대기 기록, 내부 진단/동기화 상태 보관
- `downloads`: 사용자가 요청한 자막 파일 저장
- `activeTab`: 현재 활성 탭 상태 확인
- `scripting`: 기존 탭에 확장 기능 재연결 또는 재주입
- `offscreen`: 파일 내보내기용 Blob 처리
- `https://assembly.webcast.go.kr/*`, `https://webcast.assembly.go.kr/*`: 해당 국회 중계 페이지에서만 자막 표시 및 수집

### 8. 이용자 권리

사용자는 언제든지 다음을 할 수 있습니다.

1. 저장된 자막 기록 삭제
2. 확장 설정 변경 또는 초기화
3. 확장프로그램 제거를 통한 로컬 데이터 사용 중단

### 9. 보안

확장프로그램은 Chrome 확장프로그램 권한 체계 내에서 동작하며, 필요한 범위의 권한만 사용하도록 설계되어 있습니다.  
사용자 데이터는 기본적으로 브라우저 내부에서 처리되며, 외부 서버 전송을 전제로 하지 않습니다.

### 10. 개인정보처리방침 변경

본 방침은 기능 변경 또는 정책 변경에 따라 수정될 수 있습니다. 중요한 변경이 있는 경우 공개된 정책 문서에서 최신 내용을 확인할 수 있습니다.

### 11. 시행일

- 시행일: `[시행일 예: 2026-03-12]`

---

## Notion 게시 메모

1. 위 본문을 Notion 페이지에 붙여넣습니다.
2. `[운영자명]`, `[문의 이메일]`, `[시행일]`을 실제 값으로 바꿉니다.
3. Notion 페이지를 웹 공개로 전환합니다.
4. 생성된 공개 링크를 Chrome Web Store의 `Privacy Policy URL`에 입력합니다.

## 참고

- Chrome Web Store 정책상 사용자 데이터를 다루는 경우 정확하고 최신의 개인정보처리방침 링크가 필요합니다.
- 대시보드의 privacy 필드 내용, 개인정보처리방침, 실제 확장 동작은 서로 일치해야 합니다.
