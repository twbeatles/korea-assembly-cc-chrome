# UX 개선 기록

## 목적

자막 수집 중의 핵심 행동을 한 화면에서 끝내고, 상태 메시지와 일회성 결과를 구분하며, 설정 변경의 저장 여부를 명확히 보여준다.

## 반영한 개선

### 1. 수집 제어를 페이지 패널에 배치

- 페이지 패널의 `자막 수집` 영역에서 현재 상태에 맞는 `수집 시작` 또는 `수집 종료` 버튼을 제공한다.
- 실행 중에는 종료만, 종료 상태에서는 시작만 보이도록 해 잘못된 행동을 줄인다.
- 저장·복사·내보내기와 수집 제어를 같은 작업 흐름으로 묶어 팝업과 페이지 패널 사이의 왕복을 줄인다.

### 2. 팝업의 연결 상태와 결과 알림 분리

- 연결 중, 연결됨, 재연결 시도 같은 지속 상태는 상태 안내 줄에 남긴다.
- 저장, 패널 열기, 설정 열기, 오류처럼 일회성 결과는 짧게 사라지는 피드백 메시지로 표시한다.
- 성공·정보·오류 색을 구분해 결과를 빠르게 식별할 수 있게 한다.

### 3. 설정 변경 상태 표시

- 마지막 저장본과 현재 설정을 비교해 변경 여부를 계산한다.
- 하단 영역에 `저장하지 않은 변경사항이 있습니다`, `설정을 저장하고 있습니다`, `모든 변경사항이 저장되었습니다`를 표시한다.
- 변경이 없거나 입력 오류가 있거나 저장 중일 때는 저장 버튼을 비활성화한다.

### 4. 내보내기 선택 단순화

- 가장 일반적인 `TXT 저장`을 독립된 기본 행동으로 제공한다.
- SRT, VTT, JSON, MD, CSV는 `다른 형식 선택`에 묶어 화면의 선택 밀도를 낮춘다.
- 모든 기존 형식과 내보내기 로직은 유지한다.

## 검증 기준

- 수집 패널은 상태에 맞는 시작/종료 제어를 하나만 노출한다.
- 저장·복사·내보내기는 수집된 내용이 있을 때만 활성화된다.
- 설정 저장은 실제 변경이 있을 때만 가능하다.
- 팝업의 상태 안내는 연결 상태를, 피드백 메시지는 사용자의 직전 행동 결과를 전달한다.
