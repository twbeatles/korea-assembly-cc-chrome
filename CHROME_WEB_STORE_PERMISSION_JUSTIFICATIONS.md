# Chrome Web Store Permission Justifications

Chrome Web Store 제출 폼에 바로 붙여넣을 수 있도록 간략 문안만 정리한 파일입니다.

## storage 사용 근거

확장 설정값과 저장된 자막 세션 기록을 보관하기 위해 사용합니다. 사용자가 설정한 자동 저장, 자동 스크롤, 파일명 패턴, 최근 N줄 복사 같은 옵션을 유지하고, 저장한 자막 기록의 즐겨찾기 상태와 메모를 다시 열람할 수 있게 보관합니다. JSON 가져오기로 복원한 기록도 같은 로컬 저장소에 저장합니다. 페이지 종료 직전 저장 복구를 위한 replay queue, history 화면의 최신 목록 동기화를 위한 내부 revision 신호, queue write / replay / cleanup 단계별 저장 복구 진단 정보 같은 내부 상태도 브라우저 로컬 저장소에서만 관리합니다. replay queue 본문은 가능한 경우 IndexedDB를 우선 사용하고, `chrome.storage.local` 에는 레거시 데이터와 IDB 불가 시 fallback snapshot만 유지합니다.

## unlimitedStorage 사용 근거

장시간 국회 회의처럼 자막 데이터가 오래 누적되는 경우에도 브라우저 로컬 저장 한도 때문에 세션 본문, replay queue fallback, 저장 복구 진단 정보가 조기 실패하지 않도록 사용합니다. 외부 서버로 전송하지 않고 사용자의 브라우저 내부 저장 여유만 늘리는 목적이며, 실제 저장 대상은 자막 기록과 확장 설정처럼 기능 수행에 필요한 로컬 데이터로 제한됩니다.

## downloads 사용 근거

사용자가 직접 요청한 자막 기록을 TXT, SRT, VTT, JSON 파일로 저장하기 위해 사용합니다. 자동 다운로드는 하지 않으며, 페이지 패널이나 history 화면에서 저장 버튼을 눌렀을 때만 동작합니다. 단일 세션 export와 전체 기록 JSON 백업도 모두 사용자의 명시적 요청일 때만 실행됩니다.

## offscreen 사용 근거

내보내기 파일 생성 시 Blob 기반 다운로드를 안정적으로 처리하기 위해 사용합니다. 대용량 자막 데이터를 브라우저 정책에 맞게 파일로 저장할 때만 사용하며, 사용자 요청이 있을 때만 동작합니다.

## 호스트 권한 사용 근거

`https://assembly.webcast.go.kr/*` 와 `https://webcast.assembly.go.kr/*` 페이지에서만 AI 자막 DOM을 읽고, 자막 수집 패널을 표시하며, 사용자가 보고 있는 국회 중계 자막을 저장할 수 있도록 하기 위해 사용합니다. 다른 사이트에서는 동작하지 않으며 권한 범위도 해당 두 도메인으로 제한되어 있습니다.
